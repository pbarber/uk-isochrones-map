aws --profile ${AWS_PROFILE} s3 cp --recursive East\ Midlands/ s3://${BUCKETNAME}
aws --profile ${AWS_PROFILE} s3 cp --recursive East\ Scotland/ s3://${BUCKETNAME}
aws --profile ${AWS_PROFILE} s3 cp --recursive East\ of\ England/ s3://${BUCKETNAME}
aws --profile ${AWS_PROFILE} s3 cp --recursive London\ East/ s3://${BUCKETNAME}
aws --profile ${AWS_PROFILE} s3 cp --recursive London\ West/ s3://${BUCKETNAME}
aws --profile ${AWS_PROFILE} s3 cp --recursive North\ East/ s3://${BUCKETNAME}
aws --profile ${AWS_PROFILE} s3 cp --recursive North\ Scotland/ s3://${BUCKETNAME}
aws --profile ${AWS_PROFILE} s3 cp --recursive North\ West\ North/ s3://${BUCKETNAME}
aws --profile ${AWS_PROFILE} s3 cp --recursive North\ West\ South/ s3://${BUCKETNAME}
aws --profile ${AWS_PROFILE} s3 cp --recursive Northern\ Ireland/ s3://${BUCKETNAME}
aws --profile ${AWS_PROFILE} s3 cp --recursive South\ East/ s3://${BUCKETNAME}
aws --profile ${AWS_PROFILE} s3 cp --recursive South\ West/ s3://${BUCKETNAME}
aws --profile ${AWS_PROFILE} s3 cp --recursive Wales/ s3://${BUCKETNAME}
aws --profile ${AWS_PROFILE} s3 cp --recursive West\ Midlands/ s3://${BUCKETNAME}
aws --profile ${AWS_PROFILE} s3 cp --recursive West\ Scotland\ North/ s3://${BUCKETNAME}
aws --profile ${AWS_PROFILE} s3 cp --recursive West\ Scotland\ South/ s3://${BUCKETNAME}
aws --profile ${AWS_PROFILE} s3 cp --recursive Yorkshire\ and\ the\ Humber/ s3://${BUCKETNAME}
